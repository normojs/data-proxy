# Enterprise Governance Temporary Development Queue

日期：2026-07-03

本文档只记录当前企业功能的未完成项和开发顺序。已完成的 MVP/MVP+ 功能不再重复列出。当前约束：不做线上验证、不部署生产；需要预发或生产环境证据的任务先保留在后置队列。

## 开发原则

1. 先做本地可验证、低风险、能补齐运营闭环的功能。
2. 再做需要较多 UI/权限边界的增强。
3. 最后做需要真实外部系统、预发或生产环境的验证项。
4. 每个功能线独立提交，避免混入无关改动。
5. 当前已有 `VERSION` 本地改动，不纳入企业功能提交。

## 当前开发指针

- [ ] EG-TMP-003 项目详情侧栏

## 本轮完成记录

- [x] EG-TMP-002 审计长期归档任务设计与最小实现
  - 完成内容：新增默认关闭的企业审计日志保留期清理任务；支持 `dry-run`、每批限量、主节点执行和保留天数配置；实际清理后按企业写入 `audit_log.retention_cleanup` 审计摘要；补充服务层 dry-run、删除审计、limit/has_more 回归测试。

- [x] EG-TMP-001 审计日志 CSV 导出
  - 完成内容：新增 scoped audit export API；前端 Audit 页增加 CSV 导出按钮；导出复用当前筛选条件并写入脱敏后的 before/after JSON；补充部门/项目 scope 导出回归测试。

## 第一批：本地可开发的运营闭环

- [x] EG-TMP-001 审计日志 CSV 导出
  - 目标：企业审计页支持按当前筛选导出 CSV。
  - 验收：后端新增 scoped audit export API；复用现有审计权限与 scope 过滤；前端 Audit 页提供导出入口；新增后端权限/过滤测试；前端 typecheck 通过。

- [x] EG-TMP-002 审计长期归档任务设计与最小实现
  - 目标：提供按时间归档或清理企业审计日志的基础任务，默认关闭。
  - 验收：新增配置项或环境变量；支持 dry-run；写操作审计；补服务层测试。

- [ ] EG-TMP-003 项目详情侧栏
  - 目标：项目列表可打开详情侧栏，查看部门绑定、成员、策略、用量入口和最近审计。
  - 验收：不新增不必要路由；复用现有项目、成员、用量、审计 API；前端 typecheck/build 通过。

- [ ] EG-TMP-004 项目 owner 选择器体验增强
  - 目标：创建/编辑项目时从企业成员中选择 owner，而不是只依赖手输 ID。
  - 验收：支持搜索；保持权限边界；提交 payload 仍兼容现有 API。

- [ ] EG-TMP-005 项目专属审批历史筛选与详情增强
  - 目标：Quota Requests 支持按 project_id 过滤，并在详情里显示项目上下文。
  - 验收：后端列表过滤、前端筛选、详情展示和权限测试覆盖。

- [ ] EG-TMP-006 即将过期临时额度提醒
  - 目标：已批准临时额度在过期前进入通知 outbox。
  - 验收：后台任务默认关闭或安全间隔；避免重复通知；站内/email/webhook 复用现有通知偏好；服务层测试覆盖。

## 第二批：策略和告警增强

- [ ] EG-TMP-101 alert 动作接入通知/告警渠道
  - 目标：策略 action=alert 不只写审计，也能按通知偏好投递站内/email/webhook。
  - 验收：命中 alert 后生成可去重 outbox；dry-run 不误发；测试覆盖通知偏好 gate。

- [ ] EG-TMP-102 异常检测触发后的 fallback/shared_pool 等动作执行增强
  - 目标：异常保护不只默认限流或交给 queue，也能按策略动作执行 fallback/shared_pool/alert。
  - 验收：动作优先级明确；不会绕过 hard-limit；响应 header 和审计可解释。

- [ ] EG-TMP-103 策略影响面预估
  - 目标：启用/停用/修改 hard-limit 前展示影响用户、部门、项目和最近命中量。
  - 验收：后端提供预估接口或前端组合现有数据；操作确认弹窗展示影响摘要。

- [ ] EG-TMP-104 策略条件可视化编辑器增强
  - 目标：CEL 条件常用字段可视化编辑，保留高级 JSON/CEL 输入。
  - 验收：可生成兼容现有策略条件；错误提示可读；不破坏已有策略。

## 第三批：队列和重放增强

- [ ] EG-TMP-201 更多 replay endpoint 覆盖清单和测试矩阵
  - 目标：明确当前 queue replay 已覆盖和未覆盖 endpoint。
  - 验收：文档列出 endpoint、content-type、payload 存储、风险和优先级；补至少一条未覆盖 endpoint 的测试或明确暂缓原因。

- [ ] EG-TMP-202 replay 结果更细粒度可观测
  - 目标：队列 replay 展示最终 relay request_id、失败阶段、上游状态和耗时。
  - 验收：DB 字段或 payload 兼容迁移；UI 展示；敏感字段不泄露。

- [ ] EG-TMP-203 队列优先级/权重调度
  - 目标：queue action 支持按策略、项目或部门设置优先级。
  - 验收：同企业队列内优先级可解释；低优先级不会饿死；测试覆盖排序。

## 第四批：SSO 和组织同步增强

- [ ] EG-TMP-301 批量冲突确认 UI
  - 目标：SSO Sync 冲突候选支持批量选择、批量确认和批量忽略。
  - 验收：不会误应用冲突行；apply 前能再次预览。

- [ ] EG-TMP-302 部门结构变更安全回滚增强
  - 目标：在安全范围内回滚部门创建、移动、禁用。
  - 验收：只回滚没有新依赖的结构变更；有风险的项明确 skipped reason。

- [ ] EG-TMP-303 外部身份源定时同步框架
  - 目标：为 LDAP/企业微信/飞书/钉钉/Okta 等真实连接器预留调度和运行记录。
  - 验收：默认关闭；运行记录可审计；失败不会影响手工同步。

- [ ] EG-TMP-304 LDAP 真实连接器
  - 目标：支持 LDAP 拉取部门/成员快照。
  - 验收：配置项脱敏；连接测试；dry-run preview；无真实凭据入库。

- [ ] EG-TMP-305 企业微信真实连接器
- [ ] EG-TMP-306 飞书真实连接器
- [ ] EG-TMP-307 钉钉真实连接器
- [ ] EG-TMP-308 Okta 真实连接器

## 第五批：RBAC 和角色产品化

- [ ] EG-TMP-401 角色管理 UI 独立化
  - 目标：管理员可在企业治理内分配企业管理员、部门管理员、财务、审计、项目管理员等角色。
  - 验收：角色变更写审计；不允许移除最后一个有效管理员；权限即时体现在 `/api/user/self`。

- [ ] EG-TMP-402 项目级更细粒度权限边界
  - 目标：项目成员可按 read/admin/billing/audit 等能力进一步拆分。
  - 验收：后端 capability 明确；前端入口按能力显示；回归覆盖越权。

- [ ] EG-TMP-403 自定义角色/权限模板
  - 目标：支持基于 capability 的自定义角色模板。
  - 验收：系统内置角色不可破坏；自定义角色可审计、可禁用。

## 第六批：共享池和财务增强

- [ ] EG-TMP-501 共享池周期重置策略
  - 目标：支持共享池容量按周期自动重置或补给。
  - 验收：周期边界清楚；重置写审计；不会破坏既有 borrow 归属。

- [ ] EG-TMP-502 共享池借用审批或借用上限
  - 目标：共享池可配置单次/单用户/单项目借用上限，必要时走审批。
  - 验收：超上限行为可配置；审计和提示完整。

- [ ] EG-TMP-503 成本中心分摊报表增强
  - 目标：项目、部门、模型、渠道、API Key 的成本分摊报表更完整。
  - 验收：导出字段稳定；筛选和 UI 展示一致。

- [ ] EG-TMP-504 定时报表/邮件报表
  - 目标：按日/周/月给财务或项目 owner 发送用量报表。
  - 验收：通知偏好可控；支持预览；避免重复发送。

## 第七批：高并发和压测证据

- [ ] EG-TMP-601 操作级幂等补偿队列增强
  - 目标：企业 quota reserve/settle/refund 在进程中断、DB/Redis 部分失败时有更细的幂等补偿。
  - 验收：事件状态机清晰；重复执行安全；测试覆盖失败注入。

- [ ] EG-TMP-602 真实 Redis Lua 路径持续压测证据
  - 目标：连接真实 Redis 跑并发一致性压测并记录结果。
  - 验收：记录命令、Redis 版本、并发参数、通过结果和异常处理。

- [ ] EG-TMP-603 大客户规模压测报告
  - 目标：覆盖并发、streaming、失败回滚、dry-run、hard limit、queue、shared_pool。
  - 验收：报告可复现；不使用真实敏感数据。

## 第八批：需要预发或生产环境的收口

- [ ] EG-TMP-701 当前最新企业全量 preflight 重新执行记录
  - 目标：记录当前 HEAD 的企业治理 preflight 结果。
  - 验收：命令、commit、时间、通过结果写入正式证据文档。

- [ ] EG-TMP-702 预发 R0-R3 灰度演练证据
  - 目标：在预发环境记录真实 request_id、审计、counter、attribution 和回滚结果。
  - 验收：填充 `docs/enterprise-governance-v1.3-release-evidence.md` 的预发表格。

- [ ] EG-TMP-703 生产 R0-R3 灰度演练证据
  - 目标：在生产小范围验证 hard-limit、dry-run、通知和回滚。
  - 验收：填充生产证据；证明 R3 第二次请求 403 且不打上游。

- [ ] EG-TMP-704 生产 email/webhook 真实投递证据
  - 目标：记录真实投递、签名、失败重试和偏好 gate。
  - 验收：不泄露密钥、邮箱隐私和业务 payload。

- [ ] EG-TMP-705 HStation OAuth 真实环境验证
  - 目标：验证登录、注册、绑定、解绑、回调错误、取消授权和重复绑定。
  - 验收：记录环境、回调地址、测试账号、结果和截图/变更单。

## 暂不进入近期主线

- [ ] 多节点企业治理一致性增强
- [ ] 跨节点 SSE
- [ ] 分布式 Tunnel 限流
- [ ] 分布式带宽统计
